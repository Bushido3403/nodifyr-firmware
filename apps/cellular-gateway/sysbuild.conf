# Freestanding nRF9151/ns builds include TF-M via the board target.
# Thingy:91 X factory layout enables MCUboot by default (USB DFU).
# nRF9151 DK bring-up can omit MCUboot unless you enable it here.
